[build-system]

requires = ["setuptools",
            "setuptools_scm",
            "wheel",
            "extension-helpers",
            "oldest-supported-numpy",
            "cython==0.29.14",
            "nbsphinx",
            "IPython"]

build-backend = 'setuptools.build_meta'
